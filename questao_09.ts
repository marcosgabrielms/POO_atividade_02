// Exemplo 1
let a_ts = 10;
let b_ts = "5";
console.log(a_ts + b_ts);

// O operador "+" não pode ser aplicado aos tipos NUMBER e STRING

// Exemplo 2
let x_ts = true;
let y_ts = 2;
console.log(x_ts + y_ts);

// O operador "+" não pode ser aplicado aos tipos BOOLEAN e NUMBER
